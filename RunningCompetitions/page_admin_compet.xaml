<Page x:Class="RunningCompetitions.page_admin_compet"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RunningCompetitions"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="page_admin_compet">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60">
            </RowDefinition>
            <RowDefinition>
            </RowDefinition>
            <RowDefinition Height="70">
            </RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Text="Соревнования" FontSize="36" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
        <DataGrid  x:Name="dg" ItemsSource ="{Binding result}" Grid.Row="1" SelectionChanged="ListView_SelectionChanged">
        </DataGrid>
        <Button x:Name="btn_back" Click="btn_back_Click" Grid.Row="2" Width="200" Content="Назад" Height="50" FontSize="30"></Button>
    </Grid>
</Page>
